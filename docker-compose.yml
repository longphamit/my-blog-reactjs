version: '3.7'

services:
  my-blog-reactjs:
    container_name: my-blog-reactjs
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=development